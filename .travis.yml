language: java
jdk: oraclejdk8
script:
  - gradle fatjar
deploy:
    provider: releases
    api_key: $GITHUB_OAUTH
    file: "build/libs/donorschoose-"+TRAVIS_TAG+".jar"
    skip_cleanup: true
    on:
      tags: true